robokudo.descriptors.camera_configs.config_filereader_playback
==============================================================

.. py:module:: robokudo.descriptors.camera_configs.config_filereader_playback


Classes
-------

.. autoapisummary::

   robokudo.descriptors.camera_configs.config_filereader_playback.FileReaderCameraConfig


Module Contents
---------------

.. py:class:: FileReaderCameraConfig

   Bases: :py:obj:`robokudo.descriptors.camera_configs.base_camera_config.BaseCameraConfig`


   Configuration class for file-based camera data playback.

   This class defines the configuration parameters for reading camera data from files,
   typically used for offline processing or testing. It supports reading from both
   ROS package directories and regular filesystem paths.

   .. note::
       If target_ros_package is None, target_dir is used as an absolute or relative
       path. Otherwise, target_dir is appended to the ROS package path.


   .. py:attribute:: registry_name
      :type:  typing_extensions.ClassVar[str]
      :value: 'file_reader'


      Name of the camera configuration in the registry.

      Must be unique.



   .. py:attribute:: interface_type
      :type:  str
      :value: 'FileReader'


      Type of camera interface.



   .. py:attribute:: loop
      :type:  bool
      :value: True


      Shall we loop after iterating over a directory?



   .. py:attribute:: target_ros_package
      :type:  typing_extensions.Optional[str]
      :value: None


      Files for the FileReaderInterface should be in a ROS package If you don't want that,
      leave target_ros_package to None and define target_dir either absolute or relative
      to your executable.



   .. py:attribute:: target_dir
      :type:  str
      :value: '/tmp'


      If target_ros_package is None, try to load target_dir directly.

      Otherwise, append target_dir to target_ros_package and load the files from there.



   .. py:attribute:: color2depth_ratio
      :type:  typing_extensions.Tuple[float, float]
      :value: (0.5, 0.5)


      If you have depth data to read from, please set the ratio in x,y here If it's not
      set, (1,1) will be the default.

      Set to None if you don't have that.



   .. py:attribute:: filename_prefix
      :type:  str
      :value: 'rk_'


      Define the prefix of all the files that shall be loaded into the
      FileReaderInterface.



   .. py:attribute:: kinect_height_fix_mode
      :type:  bool
      :value: True


      Apply kinect hack.



